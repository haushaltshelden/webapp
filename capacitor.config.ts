import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.haushaltshelden',
  appName: 'haushaltshelden',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
